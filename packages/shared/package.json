{
  "name": "@nusaai/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared types and utilities for NusaAI",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "dev": "tsc --watch",
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "lint": "echo 'lint placeholder'",
    "test": "echo 'test placeholder'",
    "clean": "rm -rf dist"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
